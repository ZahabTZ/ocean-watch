# Frontend runtime env vars
# Copy to .env.local (or .env) before running `npm run dev`.

# Global Fishing Watch API token used for live vessel positioning.
# Required for MapView to render vessel positions.
VITE_GFW_API_TOKEN=
